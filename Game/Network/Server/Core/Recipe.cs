using System.Collections.Generic;
namespace Tewi.Game.Network.Server.Recipe
{/*
    public class RecipeRuntime
    {
        public Dictionary<int, int> inputs;
        public Dictionary<int, int> outputs;

        public float duration;
    }

    public static class RecipeUtils
    {
        public static RecipeRuntime ToRuntime(this RecipeSO recipe)
        {
            var runtime = new RecipeRuntime
            {
                inputs = new Dictionary<int, int>(),
                outputs = new Dictionary<int, int>()
            };

            foreach (var i in recipe.inputs)
            {
                runtime.inputs[i.type.id] = i.amount;
            }

            foreach (var o in recipe.outputs)
            {
                runtime.outputs[o.type.id] = o.amount;
            }

            runtime.duration = recipe.duration;

            return runtime;
        }
    }*/
}
